apiVersion: v1
kind: Service
metadata:
  name: event-publisher-service
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  - port: 8084
    targetPort: 8084
    name: esl-socket
  selector:
    app: event-publisher
