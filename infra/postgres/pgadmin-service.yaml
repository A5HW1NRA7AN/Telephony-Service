apiVersion: v1
kind: Service
metadata:
  name: pgadmin-service
  namespace: default
  labels:
    app: pgadmin
spec:
  ports:
  - port: 80
    targetPort: 80
    name: http
  selector:
    app: pgadmin
  type: ClusterIP
